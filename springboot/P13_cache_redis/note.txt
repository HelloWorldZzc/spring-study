=========缓存（使用redis）===========
    1、导入启动缓存与加入redis服务商的相关坐标
    2、缓存的基本使用(启动类启用缓存标注、相关的注解标明)
    3、在yml文件中对使用缓存技术限定、redis技术限定

本案例特殊点：
    1、缓存信息放入redis中，可以在其中进行查询
    2、方法库中，缓存默认没有返回-1（返回null导致类型转化错误）

redis缓存的使用较为简单，与默认的几乎无差别，写在一起比较混乱，因此开一章节特意写redis缓存
=============